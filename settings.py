from maltego_trx.decorator_registry import TransformSet

cloak_n_dagger_transformset = TransformSet(name="CloakNDagger",
                                           description="Set of transformations to set up CloakNDagger")

hunchly_transformset = TransformSet(name="Hunchly",
                                    description="Set of transformations for interaction with Hunchly")

whatsmynameapp_transformset = TransformSet(name="WhatsMyNameApp",
                                           description="Set of transformation for interaction with WhatsMyNameApp")

tavily_transformset = TransformSet(name="Tavily",
                                           description="Search with Tavily")